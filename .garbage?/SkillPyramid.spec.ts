/*

import { expect, test } from "vitest";
import { mount } from "@vue/test-utils";
import SkillPyramid from "../SkillPyramid.vue";

test("mount comoponent", async () => {
  expect(SkillPyramid).toBeTruthy();

  const wrapper = mount(SkillPyramid, {
    props: {
      skills: [
        { skillLevel: 1 },
        { skillLevel: 2 },
        { skillLevel: 3 },
        { skillLevel: 4 },
      ],
    },
  });

  console.log(wrapper);

  const purpSquaresWrapper = wrapper.find('[data-test="purp-squares"]');

  //expect(purpSquares.element.value).toBe('🟪')
});

*/